<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brian.lifttracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:background="@color/colorPrimaryDark"
        android:textColor="@color/colorAccent">
        <activity
            android:name=".LandingScreen"
            android:theme="@style/NoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrackingScreen" />
        <activity
            android:name=".TimerScreen"
            android:theme="@style/NoTitle" />
        <activity
            android:name=".AddExerciseScreen"
            android:label="@string/title_activity_display_exercise"
            android:background="@color/colorPrimaryDark"
            android:textColor="@color/colorAccent"/>
        <activity
            android:name=".EditExerciseScreen"
            android:label="@string/title_activity_edit_and_delete_data_screen"
            android:background="@color/colorPrimaryDark"
            android:textColor="@color/colorAccent"
         ></activity>
    </application>

</manifest>